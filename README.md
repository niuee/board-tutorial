# 無限畫布實作示範

這個 repository 主要是為了 2024 的 iThome 鐵人賽。

主要會跟著系列文章去做各種功能的實作。

每天不同的進度會用 tag 把它標記起來。

目前的 Read me 還很簡略，可能會慢慢補上。

主要的 package script 有這些：

- `npm run dev` ：開啟一個 dev server 去把實作的內容可以跑起來在瀏覽器 preview
- `npm run test`：跑單元測試
- `npm run build`：把 library 的東西打包起來放在 build 資料夾裡